<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.teiid.connectors</groupId>
		<artifactId>accumulo</artifactId>
		<version>17.0.0-SNAPSHOT</version>
	</parent>
	<artifactId>translator-accumulo</artifactId>
	<name>Accumulo translator</name>
	<description>Integrates the query engine with Accumulo</description>
	
	<properties>
	   <!-- 1. We declare some transitive dependencies that are needed in the wildfly module.
	        
	        2. There are a lot of duplicates/conflicts between hadoop and accumulo, that we aren't going to resolve here.
	   -->
	   <basepom.check.fail-dependency>false</basepom.check.fail-dependency>

	   <basepom.check.fail-dependency-versions-check>false</basepom.check.fail-dependency-versions-check>
       <basepom.check.skip-duplicate-finder>true</basepom.check.skip-duplicate-finder>
	</properties>

	<dependencyManagement>
		<dependencies>
			<!-- Accumulo 2.1.x is built against a newer shared-library stack than the
			     versions the Teiid parent pins project-wide (slf4j 1.7, guava 27,
			     jackson 2.11.0.rc1, caffeine 2.8). Override them for this module only so
			     the Accumulo server classes launched by MiniAccumuloCluster resolve the
			     methods/classes they need, without changing the versions the rest of the
			     build uses. These newer versions remain API-compatible with the Teiid
			     code on this module's test classpath. -->
			<dependency>
				<groupId>com.fasterxml.jackson</groupId>
				<artifactId>jackson-bom</artifactId>
				<version>2.16.1</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>com.google.guava</groupId>
				<artifactId>guava</artifactId>
				<version>33.0.0-jre</version>
			</dependency>
			<dependency>
				<groupId>org.slf4j</groupId>
				<artifactId>slf4j-api</artifactId>
				<version>2.0.12</version>
			</dependency>
			<dependency>
				<groupId>com.github.ben-manes.caffeine</groupId>
				<artifactId>caffeine</artifactId>
				<version>3.1.8</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<dependencies>
		<dependency>
			<groupId>org.teiid</groupId>
			<artifactId>teiid-api</artifactId>
		</dependency>
		<dependency>
			<groupId>org.teiid</groupId>
			<artifactId>teiid-common-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.teiid</groupId>
			<artifactId>teiid-engine</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.accumulo</groupId>
			<artifactId>accumulo-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.hadoop</groupId>
			<artifactId>hadoop-client-api</artifactId>
		</dependency>
		<dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-client-runtime</artifactId>
            <scope>runtime</scope>
        </dependency>
		<dependency>
			<groupId>org.apache.thrift</groupId>
			<artifactId>libthrift</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.zookeeper</groupId>
			<artifactId>zookeeper</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-configuration2</artifactId>
		</dependency>
		<dependency>
		    <groupId>org.apache.accumulo</groupId>
            <artifactId>accumulo-minicluster</artifactId>
            <version>${version.accumulo}</version>
            <scope>test</scope>
            <exclusions>
                <!-- hibernate-validator 6.0.11.Final (pulled transitively here) declares a
                     test dependency on the non-existent org.openjfx:javafx.base:11.0.0-SNAPSHOT,
                     which is not published to any repository and breaks dependency resolution.
                     JavaFX is not used by these tests, so exclude it. -->
                <exclusion>
                    <groupId>org.openjfx</groupId>
                    <artifactId>javafx.base</artifactId>
                </exclusion>
            </exclusions>
		</dependency>
		<dependency>
		    <!-- ZooKeeper 3.9.x uses Dropwizard Metrics (com.codahale.metrics) at
		         runtime, but its own POM declares metrics-core with provided scope so
		         it is not pulled transitively. The ZooKeeper process spawned by
		         MiniAccumuloCluster needs it on the test classpath, otherwise it dies
		         with NoClassDefFoundError: com/codahale/metrics/Reservoir. -->
		    <groupId>io.dropwizard.metrics</groupId>
		    <artifactId>metrics-core</artifactId>
		    <version>4.1.12.1</version>
		    <scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<outputDirectory>target/classes</outputDirectory>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>true</filtering>
				<includes>
					<include>**/*.xml</include>
					<include>**/*.properties</include>
				</includes>
			</resource>
			<resource>
				<directory>src/main/resources</directory>
				<filtering>false</filtering>
				<excludes>
					<exclude>**/*.xml</exclude>
					<exclude>**/*.properties</exclude>
				</excludes>
			</resource>
		</resources>
	</build>
</project>
